name: CI for Financial Data Pipeline

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Pipeline Script
        run: python pipeline.py

      - name: Configure Git for pushing changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.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 }}@github.com/MouryaNaru18/financial-data-pipeline.git

      - name: Commit and Push Changes
        run: |
          git add MRVL_data.csv
          git commit -m "Update CSV with new data"
          git push origin main
